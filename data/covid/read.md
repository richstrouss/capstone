# DSCI591 COVID Data Collection

To recreate the data

* Create and activate a virtual environment `python -m venv env` and `env\scripts\activate` (in Windows)
* Install the requirements `pip install -r requirements.txt`
